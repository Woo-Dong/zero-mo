.comment.card
  .card-body
    .content= comment.content
  .row.card-body
    .col-sm
    .col-sm.text-right
      .author
        | commented on #{moment(comment.createdAt).format('YYYY-MM-DD')}
        if comment.author
          | 
          | by #[a(href=`/contests/${comment.author.id}`)= comment.author.name]
          //- if user._id === comment.author.id
          //-   a.btn.btn-sm.btn-outline-danger(href=`/contests/${comment.author._id}?_method=DELETE`)
          //-     |#[i.fa.fa-trash] Delete
